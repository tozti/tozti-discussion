{
  "name": "tozti-discussion",
  "version": "1.0.0",
  "description": "A tozti extension providing discussion threads.",
  "main": "dist/js/index.js",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.1",
    "tozti-gulp": "tozti/tozti-gulp"
  },
  "scripts": {
    "build": "gulp build",
    "watch": "gulp watch",
    "release": "NODE_ENV=production gulp build",
    "package": "npm run release && tar -zcvf `basename $PWD`.tar.gz * --exclude=src --exclude=assets --exclude=node_modules --exclude=.git",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Romain Liautaud <romain.liautaud@ens-lyon.fr>",
  "license": "AGPL-3.0"
}
